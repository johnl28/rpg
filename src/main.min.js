const CONFIG={title:"The First Dungeon DEMO"};let gameInstance=null,gameInterface=null;$(document).ready((function(){$("title").text(CONFIG.title),gameInterface=new Interface,gameInstance=new Game,document.addEventListener("keypress",(e=>gameInstance.OnKeyDown(e))),document.addEventListener("keyup",(e=>gameInstance.OnKeyUp(e)))}));
